# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# Claude Code worktrees
.claude/worktrees/

#MIDI Files
*.mid
# ...except the M5 conversion fixtures. They are inputs, not output: a clean
# 16-step monophonic clip and a chordal one, both a couple of hundred bytes.
!project_files/test_file_simple.mid
!project_files/test_file.mid

#IDE
.vscode/

#Captures
project_files/captures/

#Profiling
*.stats

# NOTE: uv.lock is deliberately NOT ignored. It is committed so that
# `uv sync --locked` reproduces an identical environment everywhere.
