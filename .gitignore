# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# Claude Code worktrees
.claude/worktrees/

#MIDI Files
*.mid
# ...except the conversion fixtures. They are inputs, not output: a clean
# 16-step monophonic clip, a chordal one, and M6's four-track song. All small.
!project_files/test_file_simple.mid
!project_files/test_file.mid
!project_files/m6-test-file.mid

#IDE
.vscode/

#Captures
project_files/captures/

#Profiling
*.stats

# Scratch output from the USB investigation scripts, written to the repo root.
# Regenerable from a capture or a project file, and one of them is 3 MB.
initial_project_hex_map.txt
ksp_clean_bytes_log.txt
ksp_handshake_replay_log.txt
ksp_raw_packets_log.txt

# NOTE: uv.lock is deliberately NOT ignored. It is committed so that
# `uv sync --locked` reproduces an identical environment everywhere.
.aider*

#Scratch Notebooks
scratch.ipynb

# USB captures. Multi-megabyte and irreproducible without the device, so each
# is distilled to a tracked tape under tests/fixtures/ by tools/make_recall_tape.py.
usb_midi_investigation/recall_sysex.jsonl
usb_midi_investigation/recall_project_2.jsonl
usb_midi_investigation/import_to_project_3.jsonl
.serena

# Swift. `swift/Package.resolved` is deliberately NOT ignored, for the same
# reason as uv.lock above: it is what pins swift-midi-file and its transitive
# dependencies to the revisions CI and this machine actually built against.
swift/.build/
DerivedData/
*.xcuserdatad
